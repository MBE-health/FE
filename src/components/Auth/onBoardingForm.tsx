import { useState } from "react";
import {
  Box,
  Grid,
  Typography,
  Button,
  TextField,
  Stepper,
  Step,
  StepLabel,
  Stack,
} from "@mui/material";

const steps = ["0", "1"];

const MultiStepForm = () => {
  return <div> multisteo</div>;
};

export default MultiStepForm;
